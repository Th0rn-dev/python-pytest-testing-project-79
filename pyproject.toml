[tool.poetry]
name = "hexlet-code"
version = "0.1.0"
description = "Загрузчик страниц (Тестирование)"
authors = ["Th0rn-dev <solnek33@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
